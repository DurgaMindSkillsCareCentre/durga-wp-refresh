name: WordPress Daily Content Refresh

on:
  schedule:
    - cron: '0 5 * * *'   # Every day 5:00 UTC (~10:30 AM IST)
  workflow_dispatch:

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests beautifulsoup4

      - name: Run refresh script
        env:
          WP_SITE: ${{ secrets.WP_SITE }}
          WP_ACCESS_TOKEN: ${{ secrets.WP_ACCESS_TOKEN }}
        run: python refresh_post.py

      - name: Update LinkedIn featured page
        env:
          WP_SITE: ${{ secrets.WP_SITE }}
          WP_ACCESS_TOKEN: ${{ secrets.WP_ACCESS_TOKEN }}
        run: python update_featured.py

      - name: Update Blogger featured page
        env:
          WP_SITE: ${{ secrets.WP_SITE }}
          WP_ACCESS_TOKEN: ${{ secrets.WP_ACCESS_TOKEN }}
        run: python update_blogger_featured.py

      - name: Email weekly summary
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "Durga WordPress Daily Refresh"
          to: smartpos.systems@gmail.com
          from: Durga WP Refresh Bot
          body: file://summary.txt
